<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        .container { max-width: 800px; margin: 0 auto; padding: 20px; }
        .product-detail { background: #f9f9f9; padding: 20px; border-radius: 10px; }
        .product-header { display: flex; gap: 30px; margin-bottom: 20px; }
        .product-image { flex: 0 0 300px; }
        .product-image img { width: 100%; max-height: 300px; object-fit: cover; border-radius: 10px; }
        .product-info { flex: 1; }
        .product-info h2 { margin-top: 0; color: #333; }
        .product-info .price { font-size: 24px; color: #28a745; font-weight: bold; margin: 10px 0; }
        .product-info .meta { color: #666; margin: 5px 0; }
        .product-description { margin-top: 20px; padding-top: 20px; border-top: 1px solid #ddd; }
        .btn { display: inline-block; padding: 10px 20px; margin: 5px; text-decoration: none; border-radius: 5px; cursor: pointer; border: none; }
        .btn-primary { background-color: #007bff; color: white; }
        .btn-warning { background-color: #ffc107; color: black; }
        .btn-danger { background-color: #dc3545; color: white; }
        .btn-secondary { background-color: #6c757d; color: white; }
        .btn:hover { opacity: 0.8; }
        .actions { margin-top: 20px; }
        .no-image { width: 100%; height: 200px; background: #ddd; display: flex; align-items: center; justify-content: center; border-radius: 10px; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>

        <div class="product-detail">
            <div class="product-header">
                <div class="product-image">
                    <% if (product.url_image) { %>
                        <img src="<%= product.url_image %>" alt="<%= product.name %>" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                        <div class="no-image" style="display:none;">Lỗi tải ảnh</div>
                    <% } else { %>
                        <div class="no-image">Không có hình ảnh</div>
                    <% } %>
                </div>
                <div class="product-info">
                    <h2><%= product.name %></h2>
                    <p class="price"><%= product.price ? product.price.toLocaleString('vi-VN') : 0 %> VNĐ</p>
                    <p class="meta"><strong>URL hình ảnh:</strong> <%= product.url_image || 'Chưa có' %></p>
                </div>
            </div>

            <div class="actions">
                <a href="/products/<%= product.id %>/edit" class="btn btn-warning">Chỉnh sửa</a>
                <form action="/products/<%= product.id %>/delete" method="POST" style="display: inline;" onsubmit="return confirm('Bạn có chắc muốn xóa sản phẩm này?')">
                    <button type="submit" class="btn btn-danger">Xóa</button>
                </form>
                <a href="/products" class="btn btn-secondary">← Quay lại danh sách</a>
            </div>
        </div>
    </div>
</body>
</html>
